<%= content_tag :div, class: 'highlightjs' do %>
<pre><code class="ruby">POST '<%= pincode_url %>/pins',
     id: $token,
     message: $message,
     phone: $phone,
     attempts: $attempts,
     expire: $expire,
     api_key: $api_key,
     sender_key: $sender_key
</pre></code>
<% end %>